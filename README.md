# AI Fitness OS — recovery repository

This repository is reserved for restoring the AI Fitness OS Sprint 4C R2 checkpoint.

Canonical recovery artifact: `ai-fitness-os-sprint4c-r2.zip` from the ChatGPT project session.

Target recovered structure:
- `apps/mobile` — React Native Android app
- `services/api` — Go API
- `services/api/migrations` — PostgreSQL migrations
- `services/api/openapi` — API contract
- `infra` — runtime infrastructure
- `scripts` — verification/release tooling
- `docs` — architecture, QA and sprint documentation

Do not rebuild the product from the older foundation until the Sprint 4C R2 recovery is completed.
